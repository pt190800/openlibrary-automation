[pytest]
asyncio_mode = auto
addopts = --alluredir=reports/allure-results --clean-alluredir -v --html=reports/report.html --self-contained-html -m "not auth"
testpaths = tests
markers =
    auth: טסט לוגין — מחוץ לריצה הרגילה, הרץ עם: pytest -m auth
